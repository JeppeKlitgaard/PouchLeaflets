#let inset-size = 1mm
#let inset-size-back = 3mm
#let inset-size-table = 1mm

#let id-col = 6.5mm
#let id-size = 10pt
#let id-text(s) = text(size: id-size, weight: "bold", s)

/// Strokes
#let stroke-cut = (
  dash: "dash-dotted",
)

#let stroke-fat = (
  dash: "solid",
  thickness: 0.40mm,
)

#let stroke-regular = (
  dash: "solid",
  thickness: 0.20mm,
)

#let stroke-fold = (
  dash: "dotted",
  thickness: 0.30mm,
)

#let stroke-spacer = (
  dash: "solid",
  thickness: 0.6mm,
)
