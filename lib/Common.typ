#let setup(doc) = {
  /// Usage:
  /// #import "65x100_AntiStaticBag.typ": setup
  /// #show: setup

  set page(
    paper: "a4",
    flipped: true,
    margin: 0.75cm,
  )

  set text(size: 10pt)

  doc
}

